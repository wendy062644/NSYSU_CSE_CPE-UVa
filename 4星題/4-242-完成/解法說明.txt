這題與換零錢問題相似，要求出n個以內的數字數量組成的最大連續數字為多少。例如: 1 4 12 21 最多使用5個數字的話，可以從1拼湊到71。
這題可以使用DP動態規劃，在找的過程中如果超過n個數量，則代表到該數就無法繼續下去，再判斷是否超過之前的最大數字，如超過的話，將該選項紀錄到陣列中。
這題比較特別的是，如果連續最大數字相同，則要選擇從後到前的最小數字，要使用字典序的概念，不過是從自尾開始比較(一開始我寫一般的字典序，udebug都正確，但送到online judge會WA，經過交叉測試後才發現的)。